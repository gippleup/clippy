import recentQuery from './local/recentQuery';
import clipped from './local/clipped';
import colortheme from './local/colortheme';

const localApi = {
  recentQuery,
  clipped,
  colortheme,
}

export default localApi;