import { SCREEN_WIDTH } from "../generic";

export const SEARCH_RESULT_WIDTH = Math.min(SCREEN_WIDTH - 20, 720);
export const SEARCH_RESULT_HEIGHT = SEARCH_RESULT_WIDTH * (9 / 16);
export const SEARCH_RESULT_MARGIN_BOTTOM = 20;
